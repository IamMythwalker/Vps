name: klien
on:
  push:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: setup klien as admin with rdp
      shell: cmd
      run: |
        echo setting up klien user with admin rights
        net user Klein pussyhunter /add
        net localgroup "Remote Desktop Users" Klein /add
        net localgroup Administrators Klein /add
        reg add "HKLM\System\CurrentControlSet\Control\Terminal Server" /v fDenyTSConnections /t REG_DWORD /d 0 /f
        netsh advfirewall firewall set rule group="Remote Desktop" new enable=Yes
        netsh advfirewall firewall add rule name="RDP TCP" dir=in action=allow protocol=TCP localport=3389
        echo klien is now full administrator
        echo can install any app - no uac prompts

    - name: cloudflare tunnel auto
      shell: cmd
      run: |
        echo setting up cloudflare tunnel
        curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe -o cloudflared.exe
        cloudflared.exe service install eyJhIjoiY2U5MWFiYmQyYWU3YjhlZjQxMmU0YWQxODJmYzUxYzciLCJ0IjoiOTU2MjhkNmItMDcyNC00ODkzLWIyOWYtYjc5ZTBiOTNkOTM3IiwicyI6IlpUZGlOR1l4TW1FdE1EY3dOUzAwWVRCaUxXRmpNemN0T1dFNFlXUXlZVEJoTlRReCJ9
        sc query Cloudflared

    - name: connection details
      shell: cmd
      run: |
        echo ==================================
        echo login info:
        echo username: klein
        echo password: Mythwalker@07
        echo full admin - install chrome or anything
        echo 1. cloudflare zero trust -> tunnels
        echo 2. tcp -> localhost:3389
        echo 3. mstsc /v:your-tunnel-url
        echo ==================================

    - name: keep alive 6 hours
      timeout-minutes: 360
      shell: cmd
      run: ping 127.0.0.1 -n 21600 >nul.
